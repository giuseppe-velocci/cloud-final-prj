After installation of PhpUnit, store all the test file (one for each class to be tested)inside ./tests folder.

Then, to exectute all tests, run this commend from a cmd line pointing to the root directory:

./vendor/bin/phpunit --bootstrap ./vendor/autoload.php tests